from .backtracking.backtracking import Backtrack
class Algorithms:
    def backtrack(self):
        if live:
            # manage live backtracking feed
            pass
        else:
            return Backtrack()
        